void Open_PaulowniaMall_F_Win_001()
{
	OPEN_MSG_WIN();
	MSG( QuickTravel );
	SEL_CHK_PAD( 6, 20 );
	int choice = SEL( PaulowniaMall_F );
	CLOSE_MSG_WIN();
	switch(choice)
	{
		case 0: // Iwatodai Dorm
			return_to_dormitory();
			break;
		case 1: // Cancel
			break;
		default:
			break;
	}
}